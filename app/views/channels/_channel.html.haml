- if current_user.user_preferences.include?(channel.name)
    = link_to( channel, class: "text-decoration-none text-secondary btn h6 m-0") do
        .d-flex.align-items-center.text-secondary
            %i.fa-solid.fa-dog.me-1
            = channel.name